<nav class="navbar navbar-expand-lg navbar-dark bg-dark bg-gradient   py-3">
  <div class="container">

    <a href="/" class="navbar-brand">Agenda</a>

    <button class="navbar-toggler" type="button"
      data-bs-toggle="collapse"
      data-bs-target="#links"
      aria-controls="links"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="links">
      <ul class="navbar-nav ms-auto  mb-2 mb-lg-0">
        <!-- Usuário já estiver autenticado -->
        <% if (user) { %>
          <li class="nav-item me-3">
            <a class="btn text-white"><%= user.name %></a>
          </li>
          <li class="nav-item me-3">
            <a href="/sair" class="btn text-white-50">Sair</a>
          </li>

        <!-- Se o usuário ainda não estiver logado. Botões de entrar e cadastrar -->
        <% } else { %>
          <li class="nav-item me-3">
            <a href="/entrar" class="btn text-white">Entrar</a>
          </li>
          <li class="nav-item">
            <a href="/cadastrar" class="btn btn-outline-light">Cadastrar</a>
          </li>
        <% } %>
        
      </ul>
    </div>

  </div>
</nav>